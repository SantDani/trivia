import { WarpTextQuestionDirective } from './warp-text-question.directive';

describe('WarpTextQuestionDirective', () => {
  it('should create an instance', () => {
    const directive = new WarpTextQuestionDirective();
    expect(directive).toBeTruthy();
  });
});
