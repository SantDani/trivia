import { CorrectIncorrectDirective } from './correct-incorrect.directive';

describe('CorrectIncorrectDirective', () => {
  it('should create an instance', () => {
    const directive = new CorrectIncorrectDirective();
    expect(directive).toBeTruthy();
  });
});
